#!/usr/bin/env node
// Generated review documents; edit bm-ships/ships.json, not the rendered table.
import fs from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=fileURLToPath(new URL('../',import.meta.url));
const manifest=JSON.parse(fs.readFileSync(path.join(root,'bm-ships/ships.json'),'utf8'));
const weapons=JSON.parse(fs.readFileSync(path.join(root,'data/game_items.json'),'utf8')).weapons;
const names=new Map(weapons.map(w=>[w.id,w.name]));
const dir=path.join(root,'docs/ship-balance');fs.mkdirSync(dir,{recursive:true});
const ships=manifest.ships.filter(s=>s.rosterState==='active');
const clean=s=>String(s??'').replaceAll('|','/').replaceAll('\n',' ');
let md='# Complete ship values — full-roster-v2\n\nGenerated from `bm-ships/ships.json`; all 172 active content records, including restricted and reserved designs. Two retired hulls are unchanged.\n\nPrices are latinum. Standing is faction-wide (neutral = independent trade). H/S are hull/shields; speed is the cruise rating; turn is the authored base before mass/size penalties. Every row has three equipment slots.\n\n';
for(const faction of [...new Set(ships.map(s=>s.faction))].sort()){
 md+=`## ${faction}\n\n| ID · ship / role | Old → new price | Standing | H / S | Cargo | Mass | Speed / turn | Range / fuel | Supplied slots |\n|---|---:|---:|---:|---:|---:|---:|---:|---|\n`;
 for(const s of ships.filter(s=>s.faction===faction).sort((a,b)=>a.cost-b.cost))md+=`| #${s.id} ${clean(s.name)} · ${clean(s.role)} | ${s.balanceReview.previousValues.cost.toLocaleString('en-US')} → ${s.cost.toLocaleString('en-US')} | ${s.purchaseRequirements.factionStanding} | ${s.hull} / ${s.shields} | ${s.cargoCapacity} | ${s.mass} | ${s.topSpeed} / ${s.handlingTurnRate} | ${s.warpRange} / ${s.fuelCapacity} | ${s.defaultWeaponSlots.map(id=>clean(names.get(id)||'Empty')).join(' / ')} |\n`;
 md+='\n';
}
md+='## Individual review reasons\n\n';
for(const s of ships)md+=`- **#${s.id} ${s.name}:** ${s.balanceReview.rationale} Availability: ${s.availabilityRegion}${s.specialVendor?`; vendor ${s.specialVendor}`:''}.\n`;
md+='\nRole inspiration and uncertainties: [LORE-SOURCES.md](LORE-SOURCES.md). Numerical tuning and conversion choices are BM gameplay decisions.\n';
fs.writeFileSync(path.join(dir,'FULL-ROSTER.md'),md);
const data=ships.map(s=>({id:s.id,name:s.name,role:s.role,faction:s.faction,price:s.cost,old:s.balanceReview.previousValues.cost,standing:s.purchaseRequirements.factionStanding,hull:s.hull,shields:s.shields,cargo:s.cargoCapacity,mass:s.mass,speed:s.topSpeed,impulse:s.impulseSpeed,turn:s.handlingTurnRate,range:s.warpRange,fuel:s.fuelCapacity,fit:s.defaultWeaponSlots.map(id=>names.get(id)||'Empty'),reason:s.balanceReview.rationale,description:s.description,region:s.availabilityRegion,vendor:s.specialVendor||null,lore:s.balanceReview.loreBasis,armed:s.armedByDefault}));
const sourceLinks=[...fs.readFileSync(path.join(dir,'LORE-SOURCES.md'),'utf8').matchAll(/\[([^\]]+)\]\((https:\/\/[^)]+)\)/g)].map(([,title,url])=>`<li><a href="${url}">${title}</a></li>`).join('');
const html=`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>BM1 / BM2 — reviewed ship balance</title>
<style>:root{color-scheme:dark;font:16px system-ui,sans-serif;background:#0b1320;color:#e8eef5}*{box-sizing:border-box}body{margin:0}main{max-width:1380px;padding:28px;margin:auto}h1{font-size:clamp(25px,4vw,42px);line-height:1.12;margin:10px 0 16px}h2{font-size:20px;margin:6px 0}p{line-height:1.6;color:#b8c8d9}.eyebrow{color:#ffce5b;font-size:12px;letter-spacing:1.5px;text-transform:uppercase}.toolbar{position:sticky;top:0;z-index:1;background:#0b1320f5;padding:14px 0;display:flex;flex-wrap:wrap;gap:10px;border-bottom:1px solid #344458}input,select{font:inherit;color:inherit;background:#182438;border:1px solid #52617a;border-radius:8px;padding:11px;min-width:140px}input{flex:2}select{flex:1}#count{padding:16px 0;color:#adc6df}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(310px,1fr));gap:18px}article{background:#121f31;border:1px solid #34465f;border-radius:12px;padding:20px;min-width:0}.tag{font-size:12px;text-transform:uppercase;letter-spacing:.7px;color:#aacbee}.role{margin:8px 0 14px;color:#c1d3e6}.price{font-size:25px;color:#ffce5b;font-weight:700}.old{font-size:13px;color:#9aaec4;font-weight:400}.gate{margin:9px 0 14px;color:#d1e2f5}dl{display:grid;grid-template-columns:1fr 1fr;gap:6px 18px;margin:14px 0}dt{color:#9dafc2;font-size:13px}dd{margin:0;text-align:right;font-variant-numeric:tabular-nums}.fit{font-size:13px;line-height:1.65;padding:10px 12px;background:#0c1727;border-radius:6px}.fit b{color:#ffdb87}details{margin-top:14px;font-size:14px}summary{cursor:pointer;color:#a7caeb}details p{font-size:14px}.stock{font-size:12px;margin-top:12px;color:#aebed0}footer{margin:30px 0;font-size:13px;color:#9aafc4}a{color:#a8d7ff}@media(max-width:440px){main{padding:15px}.grid{grid-template-columns:1fr}}</style>
<main><div class="eyebrow">BM1 / BM2 · 13 September 2026 · full-roster-v2</div><h1>Every hull has a job.</h1><p>172 reviewed hulls. Prices, faction standing, protection, cargo, handling and all three starting slots. Approved identities and artwork are preserved in the accompanying pack. Restricted content remains restricted.</p>
<p><b>Standing:</b> 0 basic · 15 working ships · 30 established captains · 50 military · 75 strategic · 100 exceptional flagships. Empty slots can be armed. Devices share the three equipment slots.</p>
<div class="toolbar"><input id="search" type="search" placeholder="Find a hull, role or weapon…" aria-label="Search ships"><select id="faction" aria-label="Filter faction"><option value="">All factions</option></select><select id="sort" aria-label="Sort ships"><option value="price">Price: low to high</option><option value="standing">Standing</option><option value="id">Hull ID</option><option value="cargo">Cargo: high to low</option><option value="hull">Hull: high to low</option><option value="range">Range: high to low</option></select></div><div id="count" role="status"></div><div class="grid" id="ships"></div><footer>These are authored game values, not canonical specifications. Research roles and explicit BM refits are distinguished in the source notes. Medical healing, spore drive, multi-vector assault and passive refueling are not included. This review works offline without external scripts or fonts.<details><summary>Lore references</summary><ul>${sourceLinks}</ul></details></footer></main>
<script>const DATA=${JSON.stringify(data).replaceAll('<','\\u003c')};const $=id=>document.getElementById(id);const esc=s=>String(s??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');const n=x=>Number(x).toLocaleString('en-US');
for(const f of [...new Set(DATA.map(s=>s.faction))].sort())$('faction').add(new Option(f,f));
function render(){const q=$('search').value.trim().toLowerCase(),f=$('faction').value,k=$('sort').value;const a=DATA.filter(s=>(!f||s.faction===f)&&(!q||[s.id,s.name,s.role,s.faction,...s.fit].join(' ').toLowerCase().includes(q))).sort((a,b)=>(['cargo','hull','range'].includes(k)?-1:1)*(a[k]-b[k])||a.id-b.id);$('count').textContent=a.length+' of '+DATA.length+' hulls';$('ships').innerHTML=a.map(s=>'<article><div class="tag">#'+s.id+' · '+esc(s.faction)+'</div><h2>'+esc(s.name)+'</h2><div class="role">'+esc(s.role)+'</div><div class="price">'+n(s.price)+' <span class="old">latinum · was '+n(s.old)+'</span></div><div class="gate">Requires <b>'+s.standing+'</b> '+esc(s.faction==='neutral'?'independent trade':s.faction)+' standing</div><dl>'+[['Hull / shields',n(s.hull)+' / '+n(s.shields)],['Cargo',n(s.cargo)],['Equipment mass',s.mass],['Speed / turn',s.speed+' / '+s.turn],['Impulse base',s.impulse],['Warp range / fuel',s.range+' / '+s.fuel]].map(([a,b])=>'<dt>'+a+'</dt><dd>'+b+'</dd>').join('')+'</dl><div class="fit">'+s.fit.map((w,i)=>'<div><b>'+(i+1)+'</b> '+esc(w)+'</div>').join('')+'</div><div class="stock">'+esc(s.region)+(s.vendor?' · '+esc(s.vendor):'')+'</div><details><summary>Role and balance reasoning</summary><p>'+esc(s.description)+'</p><p>'+esc(s.reason)+'</p><p>Basis: '+esc(s.lore)+'. Exact stats and fit are BM gameplay choices.</p></details></article>').join('');}
for(const id of ['search','faction','sort'])$(id).addEventListener(id==='search'?'input':'change',render);render();</script></html>`;
fs.writeFileSync(path.join(dir,'REVIEW.html'),html);
console.log('Generated FULL-ROSTER.md and REVIEW.html for 172 hulls.');
